const nombre = 'Ironman';
const real = 'Stark';

const normal = nombre + ' ' + real;
const template = `El apellido de ${nombre} es ${real}`;

console.log(normal);
console.log(template);

const html = `
    <h1>Hola</h1>
    <p>mundo</p>
`;

console.log(html);